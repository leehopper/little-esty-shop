<!DOCTYPE html>
<html>
  <head>
    <title>LittleEtsyShop</title>
    <%= csrf_meta_tags %>
   <%= csp_meta_tag %>
   <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
   <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <h2>Little Etsy Shop</h2>
  <h4><%= link_to 'Welcome', '/' %></h4>
  <h4><%= link_to 'Merchants', merchants_path %></h4>
  <h4><%= link_to 'Admin Dashboard', admin_path %></h4>
  <br>

  <body>
    <% flash.each do |type, msg| %>
      <div>
        <%= msg %>
      </div>
    <% end %>

    <%= yield %>

    <br><br>
    <p><b>Github Repo Name:</b> <%= api_repo.name %> </p>

    <p><b>Total Number of Closed Pull Requests:</b> <%= api_merges %></p>

    <p><b>Top 3 Contributors: </b>
    <% api_contributors.first(3).each.with_index do |contributor, index|%>
      <% if index == 2 %>
        <%= contributor.login %> (<%= contributor.contributions %> commits)
      <% else %>
        <%= contributor.login %> (<%= contributor.contributions %> commits),
      <% end %>
    <% end %>
    </p>
  </body>
</html>
